class BoxView {
    constructor(box) {
        this._svg = document.getElementsByTagName('svg')[0];
        this._box = box;
    }

    show() {
        return '<div '
        + 'style=" '
        + ""
        
    }
}

// return '<text ' // lager html basert på objektet
// + 'id="' + this._id + '" '
// + 'x="0" '
// + 'y="14" '
// + 'fill="' + this._color + '"'
// + ''
// + '>'
// + this._text
// + '</text>';